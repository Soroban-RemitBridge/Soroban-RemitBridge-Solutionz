# ---------- Secrets ----------
.env
.env.*
!.env.example
*.pem
*.key
.secrets/

# ---------- Rust / Soroban ----------
target/
**/target/
contracts/**/test_snapshots/
*.rs.bk
Cargo.lock.orig

# ---------- Node / JS / TS ----------
node_modules/
.pnpm-store/
.yarn/
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
*.tsbuildinfo
.next/
out/
dist/
build/
coverage/
.vitest/
.vercel/

# ---------- Playwright (operator console e2e) ----------
# Reports and traces are build output: the failure is reproducible from the spec.
playwright-report/
test-results/
blob-report/
.playwright/
# The signed-in state the setup project writes: a session cookie, and a real one.
admin-web/playwright/.auth/

# ---------- Expo / React Native ----------
.expo/
.expo-shared/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
mobile/ios/
mobile/android/

# ---------- Python (deploy tooling) ----------
__pycache__/
*.py[cod]
.venv/
venv/

# ---------- Editors / OS ----------
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
*~

# ---------- Generated deploy artifacts ----------
deployed-addresses*
!deployed-addresses.example.json

# Deployment configuration carries region-specific commercial settings
# (bonds, tier bands, spreads). The example is committed; a real one is not.
scripts/config/testnet.json
scripts/config/mainnet.json
